<h1>Articles</h1>

<ul>
  <% @articles.each do |article| %>
    <li>
      <%= link_to "Title: #{article.title}", article_path(article) %> || Content: <%= article.content %>
    </li>
  <% end %>
</ul>
